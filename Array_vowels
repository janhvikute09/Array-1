Q:Create an array which is going to take user input and check which elements in array vowels if it is vowels count how many vowels are present.
import java.util.*;
public class Array_vowels
{
publioc static void main(String [] args)
{
Scanner sc=new char[5];
System.out.println("enter elements");
for(int i=0;i<char-arr.length;i++)
{
char_arr[i]=sc.next().charAt(0);
}
int count=0;
for(int i=0;i<chat_arr.length;i++)
{
if(char_arr[i]=='a'||char_arr[i]=='e'||char_arr[i]=='i'||char_arr[i]=='o'||char_arr[i]=='u')
{
count++;
}
}
System.out.println("the number of vowels in given array is"+count);
}
}
